# Troubleshooting

## Promotion Failures

- evidence provenance가 불분명하면 lesson으로 승격하지 말고 `research_index_kb`에 남긴다.
- 단일 run에서만 보인 변화는 바로 `canonical_design_kb`로 올리지 않는다.
- before/after diff 없이 “좋아진 것 같다” 수준이면 `lesson`이 아니라 `finding`으로 남긴다.
- residual uncertainty가 남아 있으면 insight가 아니라 follow-up question으로 분리한다.
- patch plan은 review 대상이지 곧바로 KB apply를 의미하지 않는다.
