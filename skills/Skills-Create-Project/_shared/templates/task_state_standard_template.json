{
  "$schema_notes": {
    "_comment": "LEGACY ALIAS — 새 코드는 이 파일을 사용하지 마세요.",
    "canonical_file": "template/dispatch_state_standard_template.json",
    "role": "dispatch = canonical mutable execution-prep state",
    "migration": "task_state → dispatch_state로 이름 변경됨. 이 파일은 하위 호환 alias이며 내용을 canonical 파일과 동기화하지 않는다."
  }
}
