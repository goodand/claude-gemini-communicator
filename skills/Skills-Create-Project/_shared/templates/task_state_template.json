{
  "$schema_notes": {
    "_comment": "LEGACY ALIAS — 새 코드는 이 파일을 사용하지 마세요.",
    "canonical_files": {
      "progress": "template/task_progress_state_template.json",
      "dispatch": "template/dispatch_state_extended_template.json"
    },
    "migration": "task_state_template은 phase model + metric registry + dispatch 필드를 혼재했음. phase_enum, phase_transitions, metric_registry, aggregate_metrics, tool_use_summary → task_progress_state_template.json으로 이동. error_log, retry_count, escalated → dispatch_state_extended_template.json으로 이동.",
    "phase_model_owner": "template/task_progress_state_template.json",
    "metric_owner": "template/task_progress_state_template.json (기록) + agent-tool-benchmark skill (정의)"
  }
}
