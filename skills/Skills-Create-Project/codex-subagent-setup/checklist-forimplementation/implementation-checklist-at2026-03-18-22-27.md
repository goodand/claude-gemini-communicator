# Implementation Checklist — codex-subagent-setup

1. Create `agents/` role files modeled after existing Claude-side `agents/*.md`
2. Keep SKILL.md short and route readers to references and KB
3. Record local Codex MCP setup and multi-agent assumptions in references
4. Add one broker role and a minimal dependency-analysis role set
5. Keep this skill document-only until repeated runtime friction justifies scripts
