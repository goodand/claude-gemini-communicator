# Sync Checklist

- 문서의 표/목록/다이어그램 중 어떤 것을 canonical source로 볼지 정했는가?
- validate 함수가 문서 규칙을 실제로 집행하는가?
- 상수 dict와 문서 전이표가 일치하는가?
- "문서에만 있음"과 "코드에만 있음"을 분리 보고하는가?
- mismatch가 단순 표현 차이인지 실제 drift인지 구분했는가?
