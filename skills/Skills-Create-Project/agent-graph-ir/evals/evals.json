{
  "skill": "agent-graph-ir",
  "evaluations": [
    {
      "name": "validate_spec_detects_duplicate_ids",
      "expectation": "duplicate ids and broken router scopes are reported"
    },
    {
      "name": "renderers_are_derived_from_ir",
      "expectation": "dot and mermaid outputs are emitted from the same spec"
    },
    {
      "name": "trace_json_matches_run_summary",
      "expectation": "langfuse-compatible trace uses summary.trace_id and summary.session_id"
    },
    {
      "name": "screenshot_bridge_wraps_stdout_paths_into_run",
      "expectation": "path-per-line screenshot helper output becomes typed observe events and a partial AgentRun"
    }
  ]
}
